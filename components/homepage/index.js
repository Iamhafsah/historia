import HeroText from "./HeroText"
import Strip from "./Strip"

const HomePage = () => {
    return (
        <div>
            <HeroText/>
            <Strip/>
        </div>
    )
}

export default HomePage
